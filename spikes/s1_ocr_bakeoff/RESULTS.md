# S1 — OCR bake-off RESULTS

- **Status:** TEMPLATE (no vendor runs yet) · **Corpus:** FC-v1 (freeze via `MANIFEST.json`)
- **Brief:** [`backlog/pi/11_spike_briefs.md` §2](../../../TMEPAgent/backlog/pi/11_spike_briefs.md)
- **Scorer:** `backend/scripts/s1_scorer.py` · **Fixtures:** `backend/scripts/generate_fc_fixtures.py`

> **Governance (§5):** this spike is **DONE only when the Decision below is filled with the
> data attached.** A number without a recorded decision is not a finished spike. Failed
> thresholds trigger the named rescope, never silent acceptance.

## How to fill this

1. Freeze FC-v1 (`generate_fc_fixtures.py`) — record the manifest hash below.
2. Run each vendor out of band; assemble the blind run-dir (see `README.md`).
3. Run `s1_scorer.py --run-dir <run-dir>` and **paste its markdown table** into the
   Coverage/Table section (vendor rows stay anonymous until scores are locked).
4. Add **actual** $/1K pages (integer cents, vendor billing) and confidence-calibration
   notes — these are revealed/recorded **after** the blind coverage read.
5. Write the Decision.

## Corpus freeze

| Field | Value |
|---|---|
| Manifest path | `spikes/s1_ocr_bakeoff/fixtures/fc_v1_synthetic/MANIFEST.json` |
| Generator seed | _(fill)_ |
| FC-1 pages / FC-B docs | _(fill)_ |
| Manifest sha256 (of MANIFEST.json) | _(fill)_ |

## Coverage + table fidelity (blind — scored before cost revealed)

Thresholds: **FC-1 ≥ 0.98**, **FC-2 ≥ 0.95** page-text recall; **table F1 ≥ 0.97**, dollar
cells exact.

| Vendor | FC-1 cov | FC-2 cov | Table F1 (FC-B) | $/1K pages (actual) | Confidence calibration | FC-1 pass | FC-2 pass | Table pass |
|---|---|---|---|---|---|---|---|---|
| _vendor_a_ | | | | | | | | |
| _vendor_b_ | | | | | | | | |
| _vendor_c_ | | | | | | | | |
| _tesseract (floor)_ | | | | (free) | | | | |

_Confidence-calibration column: does the vendor's per-token/per-page confidence actually
predict its errors? (e.g. AUROC of confidence vs. correctness, or "flags 90% of misses at
threshold X"). Uninformative confidence forces more manual review — it is the §2 tiebreak._

## Decision

- **Winner:** _(vendor, or NONE)_
- **Rule applied (§2):** passes BOTH coverage thresholds at the lowest $/1K pages; **cost
  ties break on confidence calibration** (better error-prediction wins). `decide()` returns
  the lowest-cost passer, breaking cost ties lexicographically for determinism — **the
  calibration tiebreak is applied here by hand and recorded** when two vendors tie on cost.
- **Cost at ≤$8/case?** _(pages/case × $/1K → $/case; state assumption for pages/case)_
- **Rationale + attached data:** _(link the run-dir + pasted scorer table above)_

### Rescope triggers (§2, bound to `05 §0` kill criteria)

- **No vendor ≥ 0.95 FC-2 recall** → **triggered?** _(y/n)_ → MVP intake narrows to
  **text-layer + clean scans**; revisit degraded-fax quarterly.
- **FC-B F1 < 0.90 for every vendor** → **triggered?** _(y/n)_ → **billing extraction goes
  human-in-loop** (review queue) at MVP instead of shipping unreliable auto-extracted specials.

## Gold-label provenance (§2 — auditable, not asserted)

- FC-1 / FC-B gold: **generated by `generate_fc_fixtures.py`** (synthetic, no PHI) — the
  generator is the labeler; provenance banner is in each gold file header.
- FC-2 / FC-3 gold: **founder-transcribed** on a 10% stratified page sample (FC-2) /
  handwriting + checkbox capture (FC-3). Record who transcribed each label and its source
  page; **ambiguous cells/tokens adjudicated by the legal cofounder, ruling recorded here.**

| Set | Labeler | Sample | Ambiguity rulings |
|---|---|---|---|
| FC-1 | generator (synthetic) | 100% (generated) | n/a |
| FC-B | generator (synthetic) | 100% of FC-B | n/a |
| FC-2 | _(founder)_ | 10% stratified | _(link)_ |
| FC-3 | _(founder)_ | _(fill)_ | _(link)_ |
